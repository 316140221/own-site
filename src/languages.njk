---
layout: layouts/base.njk
title: Languages
rssPath: /feed.xml
---

<h1>Languages</h1>

<p class="muted">
  <a href="{{ '/feed.xml' | url }}">Site RSS</a>
</p>

{% if languages and languages.length %}
  <ul class="sources">
    {% for lang in languages %}
      <li>
        <a href="{{ ('/lang/' + lang.code + '/') | url }}">{{ lang.label }}</a>
        <span class="muted">({{ lang.code }})</span>
      </li>
    {% endfor %}
  </ul>
{% else %}
  <p class="muted">No languages detected.</p>
{% endif %}
