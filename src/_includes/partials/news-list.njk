{% if items and items.length %}
  <ul class="news-list">
    {% for item in items %}
      {% include "partials/news-item.njk" %}
    {% endfor %}
  </ul>
{% else %}
  <p class="muted" data-i18n="news.empty">No updates yet.</p>
{% endif %}
